const assertEqual = require('./assertEqual');

const countOnly = function(allItems, itemsToCount) {
  //go through objects
  //if key is found in item, 
  //add counts to their values
  //return object
  const keys = itemsToCount.keys();
  console.log(keys);

};

countOnly(['a','b','c'],{a:'', b:'', c:''});